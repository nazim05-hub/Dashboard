# Пошаговая инструкция: Загрузка проекта на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `monitor` (или любое другое имя)
   - **Description**: "Remnawave Monitor - система мониторинга серверов"
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже все есть)
4. Нажмите **"Create repository"**

## Шаг 2: Инициализируйте Git в вашем проекте

Откройте терминал (PowerShell) в корневой папке проекта (`C:\Users\ADMIN\Desktop\monitor`) и выполните:

```powershell
# Инициализация Git репозитория
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Remnawave Monitor with CI/CD"

# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/monitor.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

## Шаг 3: Проверьте, что все загрузилось

1. Обновите страницу вашего репозитория на GitHub
2. Убедитесь, что все файлы на месте
3. Перейдите в раздел **"Actions"** - там должны появиться workflows

## Шаг 4: Проверьте работу CI/CD

1. В разделе **"Actions"** на GitHub вы увидите:
   - Список всех workflows
   - Статус выполнения (желтый = выполняется, зеленый = успешно, красный = ошибка)

2. При первом push должен автоматически запуститься workflow **"CI"**

3. Если видите зеленую галочку ✅ - все работает!

## Шаг 5: Настройте переменные окружения (важно!)

⚠️ **ВАЖНО**: Файл `.env` с вашими реальными данными НЕ должен попасть в GitHub!

1. Убедитесь, что файл `backend/.env` находится в `.gitignore` (он уже там)
2. В GitHub репозитории перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Добавьте следующие Secrets (если нужно для деплоя):
   - `REMNAWAVE_API_URL` = `https://techno0.pro`
   - `REMNNAWAVE_API_KEY` = ваш API ключ

## Что происходит автоматически:

✅ При каждом push в ветки `main`/`develop`:
- Запускается тестирование backend
- Запускается тестирование frontend
- Проверяется линтинг
- Проверяются типы TypeScript
- Собираются приложения

✅ При создании тега версии (например, `v1.0.0`):
- Создается GitHub Release
- Собираются production версии
- Загружаются артефакты

✅ Еженедельно:
- Dependabot проверяет обновления зависимостей
- Создает PR с обновлениями (если есть)

## Полезные команды Git:

```powershell
# Проверить статус
git status

# Посмотреть изменения
git diff

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на ветку
git checkout main
```

## Если что-то пошло не так:

### Ошибка при push:
```powershell
# Если нужно обновить remote URL
git remote set-url origin https://github.com/YOUR_USERNAME/monitor.git
```

### Workflows не запускаются:
- Проверьте, что файлы в `.github/workflows/` загружены
- Убедитесь, что вы делаете push в ветку `main` или `develop`
- Проверьте вкладку "Actions" на GitHub

### Нужна помощь?
- Проверьте логи в разделе "Actions" на GitHub
- Все ошибки будут видны в деталях каждого workflow run

## Готово! 🎉

После выполнения этих шагов ваш проект будет на GitHub с полностью настроенным CI/CD!

